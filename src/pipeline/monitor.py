def wait_for_workflow_run(repo_url, branch, commit_sha):
    # Placeholder: always returns "completed"
    return {
        "status": "completed",
        "repo_url": repo_url,
        "branch": branch,
        "commit_sha": commit_sha
    }
